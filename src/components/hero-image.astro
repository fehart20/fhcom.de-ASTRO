---
import { Picture } from "@astrojs/image/components";
import moonImage from "~/assets/universe.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Picture
  class="object-cover w-full h-full"
  src={moonImage}
  widths={widths}
  sizes={sizes}
  alt="The surface of the moon and the universe with stars."
/>
